--- frontend/src/components/ui/Post.tsx
+++ frontend/src/components/ui/Post.tsx
@@ -169,17 +169,7 @@ export default function Post({
     return (
       <div className="flex flex-col gap-2 p-4 bg-bg-lighter rounded-xl">
         <div className="flex items-center gap-3">
           <Publisher
             username={username}
             avatarUrl={avatarUrl || `https://ui-avatars.com/api/?name=${username}&background=random`}
             className={avatarVariants({ size: isReply ? "reply" : "default" })}
           />
         </div>
         <div className="flex flex-col gap-1 w-full relative">
           <Posting
+            variant={isReply ? "comment" : "post"}
             isEditing={true}
             editPostId={id!}
             initialContent={text}
@@ -194,14 +184,10 @@ export default function Post({
   return (
-    <div className={postVariants({ background })}>
+    <figure
+      className={postVariants({
+        background,
+        size: isReply ? "reply" : "default",
+      })}
+    >
       {/* --- Colonne gauche : Avatar --- */}
-      <div className="flex-shrink-0">
-        <Publisher
-          username={username}
-          avatarUrl={avatarUrl || `https://ui-avatars.com/api/?name=${username}&background=random`}
-          size="md"
-          ring="none"
-          className={avatarVariants({ size: isReply ? "reply" : "default" })}
-        />
-      </div>
 
       {/* --- Colonne droite : Contenu --- */}
@@ -262,14 +248,16 @@ export default function Post({
             defaultCount={likesCount}
             onClick={handleLike}
           />
-          <button
-            onClick={toggleComments}
-            className="flex items-center gap-1.5 text-inactive hover:text-primary transition-colors focus:outline-none"
-            aria-label="Afficher les commentaires"
-          >
-            <IconComment className="size-5" />
-            <span className="text-14 font-medium">{localCommentsCount}</span>
-          </button>
+          {!isReply && (
+            <button
+              onClick={toggleComments}
+              className="flex items-center gap-1.5 text-inactive hover:text-primary transition-colors focus:outline-none"
+              aria-label="Afficher les commentaires"
+            >
+              <IconComment className="size-5" />
+              <span className="text-14 font-medium">{localCommentsCount}</span>
+            </button>
+          )}
 
           {user?.username === username && (
             <div className="ml-auto flex gap-3">
